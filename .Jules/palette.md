## 2024-05-24 - Accessible Icon Buttons
**Learning:** Icon-only buttons without `aria-label` or focus styles are a common pattern in this app, negatively impacting screen reader accessibility and keyboard navigation.
**Action:** Always add `aria-label` attributes and keyboard focus indicators (like `focus-visible:ring-2`) to icon-only interactive elements to improve accessibility.
